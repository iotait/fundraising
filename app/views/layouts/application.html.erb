<!DOCTYPE html>
<html>
  <head>
    <title>Fundraising App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    
    <% #This should be removed when we update the share links %>
    <script type="text/javascript" src="https://platform-api.sharethis.com/js/sharethis.js#property=5df7e5a482eea90019eed4bf&product=inline-share-buttons" async="async"></script>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e3f2fd; align-items: flex-end">
      <a class="navbar-brand" href="/"><img src="/logo.png" width="200" height=auto alt="Bookworm Logo"></a>
      <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <a class="nav-link" style="font-weight:bold; color: #3581AE" href="/">Home <span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" style="font-weight:bold; color: #3581AE" href="/dashboard/about">About</a>
          </li>
          <li class="nav-item">
            <% if user_signed_in? %>
              <a class="nav-link" style="font-weight:bold; color: #3581AE" href="/<%= current_user.type.downcase.pluralize + "/" + current_user.id.to_s%>/dashboard">Dashboard</a>
            <% end %> 
          </li>
        </ul>
        <span class="nav-link">
            <% if user_signed_in? %>
              <a style="font-weight:bold; color: #3581AE" href="/users/edit">Edit my account</a> | 
              <a style="font-weight:bold; color: #3581AE" href="/users/sign_out" data-method="delete">Log out</a>
            <% else %>
              <a style="font-weight:bold; color: #3581AE" href="/users/sign_in">Log In</a>
            <% end %>
        </span>
      </div>
    </nav>

    <div class="container-fluid">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
