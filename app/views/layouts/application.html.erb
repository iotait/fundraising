<!DOCTYPE html>
<html>
  <head>
    <title>Fundraising App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container-fluid">
      <h1><%= yield(:h1) %></h1>
    </div>

    <nav class="navbar navbar-expand navbar-dark bg-dark">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/">Home</a>
        <a class="nav-item nav-link" href="/dashboard/about">About</a>
        <% if user_signed_in? %>
          <a class="nav-item nav-link" href="/<%= current_user.type.downcase.pluralize + "/" + current_user.id.to_s%>/dashboard">Dashboard</a>
          <a class="nav-item nav-link" href="/users/sign_out" data-method="delete">Log out</a>
        <% else %>
          <a class="nav-item nav-link" href="/users/sign_in">Log In</a>
        <% end %> 
      </div>
    </nav>
    <div class="container-fluid">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
