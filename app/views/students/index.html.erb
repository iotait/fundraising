<% provide(:h1, "Students") %>
<p>This is the students page for public school's walk-a-thon fundraiser</p>
<%= link_to "Add a new student", new_student_path %>

<ul class="students">
    <% @students.each do |student| %>
    <li>Student: <%= student.name %>, Goal: <%= student.goal %></li>
    <% end %>
</ul>